server:
  port: 8001

spring:
  data:
    mongodb:
      database: TestDB
      port: 27017
      host: localhost

api :
  priceService : 'http://localhost:8002'
  getPricesByTickers : /price/getByTickers

springdoc:
  api-docs:
    path: /api-docs

logging:
  file:
    path: logs/StockService.log
  level:
    org.springframework.data.mongodb.core.MongoTemplate: DEBUG


